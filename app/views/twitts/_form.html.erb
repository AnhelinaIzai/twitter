<%= form_with(model: twitt, local: true) do |form| %>

  <% if twitt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twitt.errors.count, "error") %> prohibited this twitt from being saved:</h2>

      <ul>
      <% twitt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.text_field :title, id: :twitt_title, class:"form-control", placeholder: "Input Title" %>
  </div>

  <div class="field">
    <%= form.text_area :body, :class => "tinymce", :rows => 20, :cols => 40 %>
  </div>

  <%= form.file_field :avatars, multiple: true %>

  <div class="actions">
    <button type="submit" class="btn btn-secondary">Submit</button>
  </div>
<% end %>
